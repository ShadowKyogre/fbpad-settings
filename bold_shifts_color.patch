diff --git a/term.c b/term.c
index e30d9c0..195821b 100644
--- a/term.c
+++ b/term.c
@@ -1021,7 +1021,7 @@ static void csiseq(void)
 			}
 			setattr(args[i]);
 		}
-		if (mode & MODE_CLR8 && mode & ATTR_BOLD && !FB)
+		if (mode & MODE_CLR8 && mode & ATTR_BOLD)
 			for (i = 0; i < 8; i++)
 				if (clr16[i] == fg)
 					fg = clr16[8 + i];
